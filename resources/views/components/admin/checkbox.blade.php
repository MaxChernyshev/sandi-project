<div class="flex flex-col p-6.5">
    <div>
        <label for="{{ $for }}" class="mb-3 block text-sm font-medium text-black dark:text-white">
            {{ $label }}
        </label>
        <input type="{{ $type }}" name="{{ $name }}" id="{{ $id }}" value="{{ $value }}">
    </div>
</div>
